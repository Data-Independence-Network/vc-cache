pub fn add_polls(
    dataStream: &[u8]
) {

}

fn add_day_poll(
    vcDayId: u32,
    globalPollId: u64,
    globalCategoryIds: Vec<u64>,
) {

}

fn add_week_poll(
    vcWeekId: u32,
    globalPollId: u64,
    globalCategoryIds: Vec<u64>,
) {

}

fn add_month_poll(
    vcMonthId: u32,
    globalPollId: u64,
    globalCategoryIds: Vec<u64>,
) {

}
